{
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "concurrently \"cd ./server && yarn server --host\" \"cd ./client && yarn dev\"",
    "merge": "cd server && yarn merge"
  },
  "devDependencies": {
    "fs-extra": "^11.1.0"
  }
}
