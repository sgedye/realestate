{
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "start": "concurrently \"cd ./server && yarn server --host\" \"cd ./client && yarn dev\"",
    "merge": "cd server && yarn merge"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "devDependencies": {
    "fs-extra": "^11.1.0"
  }
}
